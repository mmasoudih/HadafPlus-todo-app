// Define and freeze the todoEnums object to prevent modifications
export const todoEnums = Object.freeze({
  status: {
    Active: "Active", // Represents an active todo
    Completed: "Completed", // Represents a completed todo
  },
});
